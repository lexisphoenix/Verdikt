# Database (SQLite for local dev — swap to Postgres URL for production)
DATABASE_URL="file:./dev.db"

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
VERIFIER_MODE=mock

# 0G Private Computer Router (https://pc.0g.ai)
VERIFIER_MODE=mock
ZERO_G_API_KEY=
ZERO_G_BASE_URL=https://router-api.0g.ai/v1
ZERO_G_MODEL=

# Hedera testnet
HEDERA_ACCOUNT_ID=
HEDERA_PRIVATE_KEY=
HEDERA_NETWORK=testnet
HEDERA_HCS_TOPIC_ID=

# ENS / Ethereum RPC
RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your-key

# Optional: OpenAI fallback when 0G unavailable
OPENAI_API_KEY=
